A lovely backend for Media-Platform ❤️❤️

npm install :-

It Reads package.json,
 Downloads all listed dependencies,
 Creates the node_modules folder

first searach nodemon in search engine :-

searach nodemon
You can  install nodemon as a development dependency. 
nodemon is a tool that helps develop Node.js based applications by automatically restarting the node application when file changes in the directory are detected.

npm i -D prettier :-

Prettier is an opinionated code formatter. It enforces a consistent style by parsing your code and re-printing it with its own rules that take the maximum line length into account, wrapping code when necessary.
searach Prettier
You can  install Prettier as a development dependency. 

 npm install cookie-parser :-

cookie-parser is a middleware,
cookie → data stored in the browser
parser → something that reads & converts raw data into usable form
So
cookie-parser = reads cookies from incoming requests

npm install cors :-

CORS is a Node.js middleware for Express/Connect that sets CORS response headers. These headers tell browsers which origins can read responses from your server.

npm i mongoose-aggregate-paginate-v2 :-

When you are using aggregation pipelines (like $match, $group, $lookup, $sort, etc.), pagination becomes more complex.so
mongoose-aggregate-paginate-v2 is a Mongoose plugin used to add pagination support to aggregate queries.

npm i jsonwebtoken :-

jsonwebtoken is a Node.js library used to create and verify JWT (JSON Web Tokens).
We mainly use it for authentication and authorization in web applications.
(Asynchronous) If a callback is supplied, the callback is called with the err or the JWT.

(Synchronous) Returns the JsonWebToken as string

npm i bcrypt :-
bcrypt is a library used to hash passwords securely before storing them in the database.

npm i multer :-

Multer is a node.js middleware for handling multipart/form-data, which is primarily used for uploading files. 

npm i cloudinary :-

Cloudinary Image and Video (Programmable Media) is a software–as-a-service (SaaS) solution for developers providing APIs that enable you to automate the entire lifecycle of your image and video assets. These APIs include dedicated Cloudinary Image and Cloudinary Video transformation, optimization, and delivery capabilities, as well as cross-media APIs for uploading, managing and analyzing assets, and managing your account.